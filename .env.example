DEBUG=true
SECRET_KEY=local-tasks-development-only
ALLOWED_HOSTS=localhost,127.0.0.1,testserver
DATABASE_URL=sqlite:///db.sqlite3
AUTH_JWKS_URL=http://localhost:8000/.well-known/jwks.json
AUTH_JWT_ISSUER=django-template-auth
# Empty for compatibility with existing auth tokens; configure both services together.
AUTH_JWT_AUDIENCE=
